# شرح تفصيلي كامل — نظام Digital Twin للتنبؤ بعمر المحرك التوربيني

**المشروع:** Turbofan Engine RUL Prediction — Digital Twin System  
**البيانات:** NASA C-MAPSS FD001  
**تاريخ:** أبريل 2026

---

## 📋 فهرس المحتويات

1. [نظرة عامة على النظام](#نظرة-عامة)
2. [بيانات التدريب](#بيانات-التدريب)
3. [حساب RUL وتقنية الـ Capping](#حساب-rul)
4. [اختيار الحساسات](#اختيار-الحساسات)
5. [هندسة المتغيرات — 89 Feature](#هندسة-المتغيرات)
6. [التقسيم والتدريب](#التقسيم-والتدريب)
7. [نتائج النموذج](#نتائج-النموذج)
8. [منطق الشغل الحالي — Runtime](#منطق-الشغل-الحالي)
9. [المحاكاة — Simulation](#المحاكاة)
10. [Dashboard — ما فيه بالضبط](#dashboard)
11. [ملفات النظام](#ملفات-النظام)
12. [مقارنة النموذج القديم والجديد](#مقارنة-النماذج)

---

## 1. نظرة عامة على النظام

النظام يتكون من **4 طبقات** متكاملة:

```
┌──────────────────────────────────────────────────────────────┐
│  Layer 1: Simulation                                         │
│  محاكاة بيانات محرك حقيقي من NASA + إضافة ضوضاء واقعية      │
└──────────────────────────┬───────────────────────────────────┘
                           │ قراءات 10 حساسات لكل دورة
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 2: Feature Engineering                                │
│  تحويل 10 قراءات خام → 89 متغير يصف التدهور                 │
└──────────────────────────┬───────────────────────────────────┘
                           │ 89 feature مهندسة
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 3: ML Model (XGBoost)                                 │
│  التنبؤ بعدد الدورات المتبقية (RUL)                           │
└──────────────────────────┬───────────────────────────────────┘
                           │ RUL المتوقع + SHAP Explanation
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 4: Streamlit Dashboard                                │
│  عرض تفاعلي: رسوم بيانية، تنبيهات، تفسير هندسي              │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. بيانات التدريب

### المصدر: NASA C-MAPSS FD001

| الملف | الوصف | الحجم |
|-------|-------|-------|
| `NASA_turbofan_train_FD001.csv` | بيانات 100 محرك من البداية حتى العطل | 20,631 صف |
| `test_FD001.txt.xlsx` | بيانات 100 محرك أخرى (مقطوعة قبل العطل) | 13,096 صف |
| `RUL_FD001.txt.xlsx` | القيمة الحقيقية لـ RUL لآخر دورة في كل محرك test | 100 قيمة |

### بنية كل صف:

```
engine_id | cycle | setting_1 | setting_2 | setting_3 | s1 | s2 | ... | s21
    1     |   1   |   -0.0007 |   -0.0004 |   100.0   |... |... | ... | ...
    1     |   2   |    0.0019 |   -0.0003 |   100.0   |... |... | ... | ...
   ...
    1     | 192   |    0.0014 |    0.0006 |   100.0   |... |... | ... | ...
    2     |   1   |   -0.0001 |   -0.0004 |   100.0   |... |... | ... | ...
```

- كل صف = دورة تشغيل واحدة لمحرك معين
- المحرك 1 عاش 192 دورة، المحرك 2 عاش دوراً مختلفاً، وهكذا
- متوسط عمر المحركات: **206 دورة** (من 128 إلى 362)

---

## 3. حساب RUL وتقنية الـ Capping

### أ. حساب RUL الأساسي

```python
# RUL = كم دورة تبقى حتى العطل
max_cycle = أطول دورة وصل إليها هذا المحرك
RUL = max_cycle - current_cycle

# مثال: محرك عاش 200 دورة، نحن في الدورة 150:
RUL = 200 - 150 = 50 دورة متبقية
```

### ب. تقنية RUL Capping (التحسين الأهم في v2)

```
بدون Capping:
  RUL range = [0 → 361]  ← 38.9% من البيانات كانت RUL > 125

مع Capping (cap=125):
  RUL = min(RUL, 125)
  RUL range = [0 → 125]  ← النموذج يركز على المنطقة الحرجة فقط
```

**لماذا هذا مهم جداً؟**

المحرك الصحي جداً لا فائدة من التمييز بين RUL=150 و RUL=361.
المهم هو الدقة في المنطقة الحرجة (0 → 125 دورة).
بعد الـ Capping، النموذج يُخصص كل طاقته للتمييز في هذه المنطقة.

```
قبل:  الدورة 10 من محرك عمره 362 → RUL=352 (ضوضاء للنموذج)
بعد:  الدورة 10 من محرك عمره 362 → RUL=125 (نفس أي محرك صحي آخر)

قبل:  الدورة 100 من محرك عمره 200 → RUL=100 (لا تغيير)
بعد:  الدورة 100 من محرك عمره 200 → RUL=100 (لا تغيير — لم يُقطع)
```

---

## 4. اختيار الحساسات

### الحساسات الثابتة (مستبعدة — 11 حساس)

```python
# هذه الحساسات std ≈ 0 → لا تحمل معلومات → تُربك النموذج
ثابتة: s1, s5, s6, s10, s16, s18, s19
منخفضة التباين جداً: s2, s8, s9, s13
```

### الحساسات المستخدمة (10 حساسات)

| الحساس | الاسم الفيزيائي | اتجاه التدهور | أهمية |
|--------|----------------|--------------|-------|
| **s3** | HPC Outlet Temperature | ⬆️ يرتفع | حراري |
| **s4** | LPT Outlet Temperature | ⬆️ يرتفع | حراري |
| **s7** | HPC Outlet Pressure | ⬇️ ينخفض | ضغط |
| **s11** | HPC Static Pressure | ⬇️ ينخفض | ضغط |
| **s12** | Fuel Flow Ratio | ⬆️ يرتفع | كفاءة |
| **s14** | Corrected Core Speed | ⬇️ ينخفض | ميكانيكي |
| **s15** | Bypass Ratio | ⬇️ ينخفض قليلاً | هوائي |
| **s17** | Bleed Enthalpy | ⬆️ يرتفع قليلاً | هوائي |
| **s20** | HPC Cooling Flow | ⬇️ ينخفض | **تبريد — الأهم** |
| **s21** | LPT Cooling Flow | ⬇️ ينخفض | **تبريد — الأهم** |

---

## 5. هندسة المتغيرات — 89 Feature

### لماذا نهندس المتغيرات؟

```
مشكلة: القراءة اللحظية لـ s20 = 38.5 → لا أعرف هل هذا طبيعي أم لا!
الحل:  s20_rmean_10 = 38.9 → s20_slope_10 = -0.04 → s20_cum_deg = 2.3
       الآن أعرف: s20 ينخفض بشكل مستمر ومتسارع!
```

### أ. Rolling Statistics — 40 Feature

```python
# لكل حساس × 2 نافذة × (mean + std) = 10 × 2 × 2 = 40 feature

s20_rmean_5   = متوسط s20 في آخر 5 دورات
s20_rmean_10  = متوسط s20 في آخر 10 دورات
s20_rstd_5    = تذبذب s20 في آخر 5 دورات   ← يكشف عدم الاستقرار
s20_rstd_10   = تذبذب s20 في آخر 10 دورات
```

**الغرض:** إزالة الضوضاء وكشف الاتجاه الحقيقي.

### ب. Temporal Dynamics — 20 Feature

```python
# لكل حساس: delta + lag1 = 10 × 2 = 20 feature

s20_delta = s20(cycle_t) - s20(cycle_t-1)
# إذا delta = -0.1 → ينخفض بسرعة → علامة تدهور
# إذا delta = +0.02 → ارتداد طبيعي

s20_lag1 = قيمة s20 في الدورة السابقة
# يساعد النموذج يفهم السياق الزمني
```

### ج. Cooling Efficiency — 3 Features

```python
cooling_ratio = s20 / s21
# نسبة بين نظامي التبريد — في المحرك الصحي ≈ 1.67
# إذا انحرف كثيراً → خلل في أحد النظامين

cooling_divergence = |s20 - s21|
# الفجوة المطلقة — تكبر عند الخلل

cooling_norm_imbalance = (s20 - s21) / (s20 + s21)
# الاختلال المعياري (بين -1 و +1)
```

**لماذا مهمة؟**
في المحرك الصحي، s20 و s21 يتدهوران معاً بشكل متزامن.
عند الخلل، أحدهما يتدهور أسرع من الآخر → الـ divergence تكبر.

### د. Degradation Slopes — 3 Features

```python
# ميل أفضل خط مستقيم على آخر 10 دورات (linear regression)
# لـ s14, s20, s21 فقط (الأكثر حساسية)

s20_slope_10 = numpy.polyfit(cycles[-10:], s20[-10:], deg=1)[0]

# slope = -0.02  → تدهور بطيء طبيعي
# slope = -0.15  → تدهور سريع ← خطر!
# slope = +0.01  → استقرار (نادر)
```

**مثال:**
```
Cycle 41: s20=38.9  ─┐
Cycle 42: s20=38.8   │
Cycle 43: s20=38.7   │ → slope = -0.10 لكل دورة
Cycle 44: s20=38.6   │
Cycle 45: s20=38.5  ─┘
```

### هـ. Cumulative Degradation — 3 Features (جديد في v2)

```python
# لـ s14, s20, s21
s20_cum_deg = Σ|s20(t) - s20(t-1)| من الدورة 1 حتى الآن

# Cycle 10:  cum_deg = 0.3  (تذبذب طبيعي)
# Cycle 50:  cum_deg = 1.8  (تراكم متوسط)
# Cycle 100: cum_deg = 5.2  (تراكم كبير)
# Cycle 180: cum_deg = 12.7 (تراكم شديد — محرك متعب)
```

**لماذا هذه Feature ثورية؟**

تخيل محركين، كلاهما s20=38.5 الآن:
- المحرك A: وصل لـ 38.5 بشكل تدريجي هادئ → cum_deg = 1.2 → لا يزال بخير
- المحرك B: وصل لـ 38.5 مع تذبذبات عنيفة → cum_deg = 8.7 → يعاني!

القيمة الحالية متشابهة لكن التاريخ مختلف — الـ cum_deg يُميّز بينهما.

### و. EWM (Exponentially Weighted Mean) — 10 Features (جديد في v2)

```python
# لـ s14, s20, s21, s3, s4 × spans=[10, 20] = 5 × 2 = 10 feature

s20_ewm_10 = متوسط متحرك أسّي span=10
s20_ewm_20 = متوسط متحرك أسّي span=20

# الفرق عن Rolling Mean العادي:
# Rolling Mean: كل الدورات في النافذة لها نفس الوزن
# EWM:          الدورة الأخيرة أكثر وزناً، كلما بعُدنا قل الوزن

# span=10: الدورة الأخيرة وزنها 18.2%, قبلها 14.9%, قبلها 12.2%, ...
# span=20: الدورة الأخيرة وزنها 9.5%,  قبلها 8.6%,  قبلها 7.8%,  ...
```

**لماذا EWM أفضل؟**
```
سيناريو: محرك يتدهور فجأة في آخر 5 دورات

Rolling Mean (window=10):
  القراءات = [38.9, 38.8, 38.8, 38.9, 38.8, 38.5, 38.3, 38.1, 37.9, 37.7]
  المتوسط = 38.47  ← لم يلاحظ بشكل كافٍ!

EWM (span=10):
  يُعطي وزناً أكبر للقراءات الأخيرة [38.5→37.7]
  المتوسط = 38.1  ← لاحظ التدهور بشكل أسرع!
```

### ملخص عدد الـ Features

| النوع | العدد | الصيغة |
|-------|-------|-------|
| Raw sensors | 10 | s3, s4, ..., s21 |
| Rolling mean | 20 | 10 sensors × 2 windows |
| Rolling std | 20 | 10 sensors × 2 windows |
| Delta | 10 | 10 sensors |
| Lag-1 | 10 | 10 sensors |
| Cooling | 3 | ratio, divergence, imbalance |
| Slopes | 3 | s14, s20, s21 |
| Cumulative Degradation | 3 | s14, s20, s21 |
| EWM | 10 | 5 sensors × 2 spans |
| **المجموع** | **89** | |

---

## 6. التقسيم والتدريب

### أ. التقسيم الصحيح (Leakage-Free)

```python
# ❌ خاطئ — data leakage:
train = أول 80% من الصفوف
val   = آخر 20% من الصفوف
# المشكلة: المحرك 1 موجود في train وval في نفس الوقت
# النموذج "يحفظ" المحركات ويبدو دقيقاً بشكل مزيف

# ✅ صحيح — GroupShuffleSplit:
train = 80 محرك كاملين (جميع دوراتهم من 1 حتى EOL)
val   = 20 محرك كاملين (مختلفين تماماً عن train)
# النموذج لا يرى هذه المحركات أبداً → تقييم حقيقي
```

### ب. إعدادات XGBoost المحسنة

```python
model = XGBRegressor(
    n_estimators          = 2000,    # حد أعلى — سيتوقف مبكراً
    learning_rate         = 0.02,    # تعلم بطيء وأدق
    max_depth             = 6,       # عمق الشجرة — يمنع overfitting
    min_child_weight      = 5,       # أدنى عينات في ورقة الشجرة
    subsample             = 0.8,     # 80% من البيانات لكل شجرة
    colsample_bytree      = 0.8,     # 80% من المتغيرات لكل شجرة
    colsample_bylevel     = 0.8,     # 80% من المتغيرات في كل مستوى
    reg_alpha             = 1.0,     # L1 regularization (Lasso)
    reg_lambda            = 5.0,     # L2 regularization (Ridge)
    gamma                 = 0.1,     # حد أدنى للتحسين عند التفرع
    objective             = "reg:squarederror",  # MSE loss
    early_stopping_rounds = 80,      # توقف إذا لم يتحسن 80 شجرة متتالية
)
```

**لماذا هذه الإعدادات؟**

| الإعداد | القيمة القديمة | القيمة الجديدة | السبب |
|---------|--------------|--------------|-------|
| `learning_rate` | 0.05 | 0.02 | تعلم أبطأ = دقة أعلى |
| `early_stopping_rounds` | 30 | 80 | صبر أكبر → لا نتوقف مبكراً |
| `reg_lambda` | 1.0 | 5.0 | regularization أقوى → تعميم أفضل |
| `min_child_weight` | 1 | 5 | مقاومة أفضل للـ overfitting |

### ج. سير التدريب

```
Iteration   0: RMSE=40.96 (نقطة البداية)
Iteration 100: RMSE=15.56 (تحسن سريع)
Iteration 500: RMSE=14.30 (تباطأ)
Iteration 900: RMSE=14.15 (تباطأ أكثر)
Iteration 1300: RMSE=14.09 (تقارب)
Iteration 1425: RMSE=14.07 ← أفضل نقطة (early stopping هنا)
Iteration 1505: RMSE=14.08 ← بدأ يسوء → التوقف
```

---

## 7. نتائج النموذج

### النموذج القديم (v1) مقابل الجديد (v2)

| المقياس | v1 (القديم) | v2 (الجديد) | التحسين |
|---------|------------|------------|--------|
| **Validation MAE** | 24.40 cycles | **9.33 cycles** | ↓ 62% |
| **Validation RMSE** | 33.66 cycles | **14.05 cycles** | ↓ 58% |
| **Test MAE** | 23.79 cycles | **12.94 cycles** | ↓ 46% |
| **Test RMSE** | 32.74 cycles | **17.62 cycles** | ↓ 46% |
| **Sim MAE (Real-Data)** | 40.40 cycles | **19.58 cycles** | ↓ 52% |
| **Near Failure MAE** | 110.50 cycles | **13.05 cycles** | ↓ 88% |

### تفصيل الدقة حسب نطاق RUL

| نطاق RUL | v1 | v2 | الأهمية |
|----------|----|----|---------|
| Early Life (150-200) | 15.87 | 45.62 | منخفضة — المحرك صحي |
| Mid-Life (100-150) | 18.20 | 11.09 | متوسطة |
| Late Life (50-100) | 60.07 | 13.38 | **عالية** |
| **Near Failure (0-50)** | 110.50 | **13.05** | **الأعلى — قرب العطل** |

> **ملاحظة:** التحسين الأهم هو Near Failure — هذا هو الوقت الذي نحتاج فيه لأعلى دقة ممكنة لاتخاذ قرار الصيانة.

---

## 8. منطق الشغل الحالي — Runtime

### دورة حياة كل Cycle في الـ Dashboard

```
المستخدم يضغط "▶️ Next Cycle"
              │
              ▼
┌─────────────────────────────────────────────────────┐
│  STEP 1: simulator.step()                           │
│  ─────────────────────────────────────────────────  │
│  يقرأ الصف التالي من بيانات محرك حقيقي             │
│  s20_real = 38.45                                   │
│  يضيف ضوضاء: noise = Normal(0, 0.09)               │
│  s20_measured = 38.45 + 0.02 = 38.47               │
└───────────────────────┬─────────────────────────────┘
                        │ {s3:1591.2, s4:1401.1, ...}
                        ▼
┌─────────────────────────────────────────────────────┐
│  STEP 2: تجميع التاريخ                               │
│  ─────────────────────────────────────────────────  │
│  history_df.append(new_row)                         │
│  الآن history_df يحتوي 46 دورة                     │
└───────────────────────┬─────────────────────────────┘
                        │ DataFrame (46 صف × 12 عمود)
                        ▼
┌─────────────────────────────────────────────────────┐
│  STEP 3: feature_engineer.transform(history_df)     │
│  ─────────────────────────────────────────────────  │
│  10 sensors → 89 features                          │
│  Rolling → Delta → Cooling → Slopes → CumDeg → EWM │
└───────────────────────┬─────────────────────────────┘
                        │ 89 feature لكل الـ 46 دورة
                        ▼
┌─────────────────────────────────────────────────────┐
│  STEP 4: scaler.transform(features)                 │
│  ─────────────────────────────────────────────────  │
│  كل feature → (value - mean_train) / std_train      │
│  نفس الـ scaler المحفوظ من وقت التدريب              │
└───────────────────────┬─────────────────────────────┘
                        │ features مُعيّرة (mean=0, std=1)
                        ▼
┌─────────────────────────────────────────────────────┐
│  STEP 5: xgb_model.predict(scaled_features)         │
│  ─────────────────────────────────────────────────  │
│  1425 شجرة قرار تصوّت                               │
│  متوسط الأصوات = 87.3                               │
│  clip(0, 125) → predicted_RUL = 87.3               │
└───────────────────────┬─────────────────────────────┘
                        │ RUL المتوقع
                        ▼
┌─────────────────────────────────────────────────────┐
│  STEP 6: shap.TreeExplainer(last_row)               │
│  ─────────────────────────────────────────────────  │
│  لكل feature: ما مقدار تأثيره على هذا التنبؤ؟      │
│  s20_slope_10:  SHAP = -18.3 ← ينقص RUL بـ 18      │
│  s20_ewm_10:    SHAP = -12.7                        │
│  s14_rmean_10:  SHAP = -9.4                         │
│  cooling_div:   SHAP = -5.8                         │
└───────────────────────┬─────────────────────────────┘
                        │ تفسير هندسي
                        ▼
                  Streamlit يُحدّث الـ UI
```

---

## 9. المحاكاة — Simulation

### ثلاثة أنواع من المحاكاة (تطورت عبر المشروع)

#### النوع 1: Physics-Based (الأصلية)
```python
# معادلة تدهور بسيطة
s20(t) = s20(t-1) - drift_rate × (1 + 0.5 × progress)

# المشكلة: بسيطة جداً → لا تعكس التعقيد الحقيقي
# MAE على محرك كامل = 51.16 دورة
```

#### النوع 2: Physics-Based Improved
```python
# تدهور غير خطي + اقتران بين الحساسات
acceleration = 1.0 + 2.0 × (progress²)  # تسارع أسّي

# أفضل قليلاً → MAE = 45.91 دورة
```

#### النوع 3: Real-Data Simulator (المستخدمة في Dashboard)
```python
# يقرأ بيانات حقيقية من ملف التراين + يضيف ضوضاء فقط
s20_measured = s20_real(cycle_t) + Normal(0, std × 0.05)

# MAE = 19.58 دورة ← أفضل بكثير!
# لأنه يعكس أنماط التدهور الحقيقية بدقة
```

### لماذا Real-Data أفضل بكثير؟

```
Physics-Based:
  نفترض: s20 ينخفض 0.01 في كل دورة
  الواقع: أحياناً 0.005, أحياناً 0.02, أحياناً لا ينخفض!

Real-Data:
  نأخذ: قيمة s20 الحقيقية من البيانات
  نضيف: ضوضاء صغيرة لمحاكاة عدم الدقة في القياس
  النتيجة: أنماط تدهور حقيقية 100%
```

---

## 10. Dashboard — ما فيه بالضبط

### الشريط الجانبي (Sidebar)

```
┌─────────────────────────────┐
│  🎛️ Simulation Controls     │
│  ─────────────────────────  │
│  Engine ID: 1               │
│  Current Cycle: 45/192      │
│  True RUL: 147 cycles       │
│                             │
│  [▶️ Next Cycle] [🔄 Reset]  │
│                             │
│  ⏩ Fast Forward             │
│  [──●──────────] 10 cycles  │
│  [   Run Cycles   ]         │
│                             │
│  🔧 Select Engine           │
│  [ Engine 1  ▼]             │
│  [   Load Engine   ]        │
│                             │
│  💾 Export Data              │
│  [ Download History CSV ]   │
└─────────────────────────────┘
```

### وظيفة كل عنصر في الـ Sidebar

| العنصر | الوظيفة |
|--------|---------|
| **Engine ID** | يعرض رقم المحرك الحالي المُحاكى |
| **Current Cycle** | الدورة الحالية / إجمالي دورات هذا المحرك |
| **True RUL** | الـ RUL الحقيقي من البيانات (ليس المتوقع) |
| **▶️ Next Cycle** | تشغيل دورة واحدة → يُحدّث كل الـ Dashboard |
| **🔄 Reset** | إعادة المحرك الحالي من الدورة 1 |
| **⏩ Run N Cycles** | تشغيل سريع بدون عرض وسطي (1-50 دورة) |
| **🔧 Select Engine** | تغيير إلى محرك مختلف من الـ 100 محرك المتاحة |
| **💾 Download CSV** | تصدير كل تاريخ الدورات + التنبؤات |

---

### القسم 1: Engine Health Status

```
┌────────────┬──────────────┬─────────────────────┬─────────────────┐
│ Engine: 1  │ Cycle: 45/192│ Pred RUL (cap=125)  │ Health Status   │
│            │              │ 87 cycles  ±8 error │ ✅ HEALTHY       │
└────────────┴──────────────┴─────────────────────┴─────────────────┘
```

**Health Status Logic:**
```python
if true_rul > 100:   → ✅ HEALTHY  (عمل عادي)
elif true_rul > 50:  → ⚠️ WARNING  (مراقبة مكثفة)
else:                → 🚨 CRITICAL (توقف فوري للفحص)
```

---

### القسم 2: Current Sensor Readings

```
┌──────────────────────────────────────────────────┐
│  📡 Current Sensor Readings                      │
│  ──────────────────────────────────────────────  │
│  Sensor  │  Value   │  Sensor  │  Value          │
│  s3      │ 1591.2   │  s4      │ 1401.5          │
│  s7      │  553.1   │  s11     │   47.5          │
│  s12     │  521.8   │  s14     │ 8138.2          │
│  s15     │    8.4   │  s17     │  393.4          │
│  s20     │   38.6   │  s21     │   23.3          │
└──────────────────────────────────────────────────┘
```

قيم الـ 10 حساسات للدورة الحالية (بعد إضافة الضوضاء).

---

### القسم 3: RUL Over Time

رسم بياني يُظهر تطور RUL مع الوقت:

```
RUL
200 │╲
180 │ ╲  True RUL (أخضر)
160 │  ╲
140 │   ╲
120 │    ╲   - - - Predicted RUL (أحمر متقطع)
100 │     ╲ - -╱╲- -
 80 │      ╲╱    ╲
 60 │             ╲
 40 │              ╲
 20 │               ╲
  0 └──────────────────────────────── Cycle
     0    50   100   150   200
```

- **الخط الأخضر الصلب:** True RUL — ينزل بشكل منتظم
- **الخط الأحمر المتقطع:** Predicted RUL — يتبع التنبؤ مع هامش خطأ
- **المنطقة الرمادية:** ±20 دورة — نطاق عدم اليقين

---

### القسم 4: Sensor Degradation Trends

5 رسوم بيانية تُظهر تطور الحساسات الحرجة:

```
s20 (HPC Cooling)      s21 (LPT Cooling)      s14 (Core Speed)
39.0┤╲                23.4┤╲                  8140┤
38.5┤ ╲               23.0┤ ╲                 8120┤╲
38.0┤  ╲__            22.6┤  ╲__              8100┤ ╲__
37.5┤    ╲___         22.2┤    ╲___           8080┤    ╲___
    └────────Cycle        └────────Cycle          └────────Cycle

s3 (HPC Temp)          s4 (LPT Temp)
1590┤                  1400┤
1595┤      _╱          1405┤      _╱
1600┤    _╱            1410┤    _╱
1605┤  _╱              1415┤  _╱
    └────────Cycle         └────────Cycle
```

**ما نلاحظه:**
- s20, s21, s14: **تنخفض** مع الوقت (تدهور)
- s3, s4: **ترتفع** مع الوقت (تراكم حراري بسبب فقدان التبريد)

---

### القسم 5: Cooling Efficiency Trends

```
Cooling Flow: s20 vs s21          Cooling Divergence
                                      │
s20 ─────────────────╲           1.2 ┤              ╱
s21 ─────────────────╲╲          0.8 ┤          ╱╱
                       ╲╲        0.4 ┤      ╱╱╱
                                 0.0 ┤──────
    └───────────Cycle               └───────────Cycle
```

**ما يُظهره:**
- كيف يتدهور نظاما التبريد مع الوقت
- هل التدهور متماثل (صحي) أم غير متماثل (خلل)
- الـ Divergence تكبر = أحد النظامين يفشل أسرع من الآخر

---

### القسم 6: SHAP Explainability — تفسير الـ AI

هذا أهم قسم للمهندسين — يُجيب على سؤال: **"لماذا النموذج قال هذا الـ RUL بالضبط؟"**

```
🔍 AI Explanation — What's driving this prediction?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Predicted RUL: 87 cycles

Top 10 Contributing Features:

Feature               SHAP Value   Effect
─────────────────────────────────────────────────────
s20_slope_10          -18.3  ◄── تبريد ينهار (أكبر خطر)
s20_ewm_10            -12.7  ◄── متوسط تبريد منخفض
s14_rmean_10           -9.4  ◄── سرعة في تراجع مستمر
s20_cum_deg            -7.2  ◄── تراكم تآكل تبريد
cooling_divergence     -5.8  ◄── اختلال توازن التبريد
s3_ewm_10              +4.1  ─►  حرارة HPC معقولة
s4_delta               -3.6  ◄── حرارة LPT ترتفع فجأة
s21_cum_deg            -2.9  ◄── تآكل تبريد LPT
s7_rmean_10            +2.1  ─►  ضغط HPC لا يزال جيداً
s11_rstd_5             -1.8  ◄── ضغط ساكن غير مستقر

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Engineering Interpretation:
  السبب الرئيسي: تبريد HPC يتدهور بسرعة (s20_slope منخفض)
  السبب الثانوي: تآكل ميكانيكي في النواة (s14 تنخفض)
  التوصية: فحص ممرات هواء التبريد في HPC + شفرات التوربين
  الإطار الزمني المقترح للصيانة: خلال 20-30 دورة
```

**كيف يُفسَّر SHAP؟**
```
SHAP = -18.3 → هذه الـ feature تُقلّل التنبؤ بـ 18.3 دورة
SHAP = +4.1  → هذه الـ feature تُزيد التنبؤ بـ 4.1 دورة

المجموع = base_value + Σ(SHAP values) = predicted_RUL
```

---

## 11. ملفات النظام

```
RouiaProject/
│
├── 📊 model/                          ← Artifacts المحفوظة
│   ├── xgb_model.joblib              ← النموذج المدرب (1425 شجرة)
│   ├── scaler.joblib                  ← StandardScaler من التدريب
│   ├── feature_cols.json              ← قائمة الـ 89 feature بالترتيب
│   └── sensor_config.json             ← baselines + drift rates + rul_cap=125
│
├── 🔧 simulation/
│   ├── __init__.py
│   ├── engine_simulator.py            ← محاكاة فيزيائية أصلية
│   ├── engine_simulator_improved.py   ← محاكاة فيزيائية محسنة
│   └── engine_simulator_real_data.py  ← ✅ مستخدمة في Dashboard
│
├── ⚙️ pipeline/
│   ├── __init__.py
│   └── feature_engineering.py        ← FeatureEngineer (89 feature)
│
├── 🤖 model/
│   ├── __init__.py
│   └── model_loader.py               ← RULPredictor (predict + SHAP)
│
├── 🖥️ app.py                          ← Streamlit Dashboard الكامل
│
├── 📝 export_model_v2.py              ← سكريبت إعادة تدريب النموذج
│
├── 📓 RUL_Prediction_Pipeline.ipynb   ← Notebook التدريب الأصلي
│
├── 📄 README.md                       ← دليل الاستخدام (إنجليزي)
├── 📄 RUL_Pipeline_Documentation.md   ← توثيق هندسي مفصل
└── 📄 System_Explanation_Arabic.md    ← هذا الملف
```

---

## 12. مقارنة النماذج

### الفروق بين v1 و v2

| الجانب | النموذج v1 | النموذج v2 |
|--------|-----------|-----------|
| **RUL Capping** | لا يوجد (0→361) | ✅ cap=125 |
| **عدد Features** | 76 | 89 (+13) |
| **Cumulative Degradation** | لا يوجد | ✅ 3 features |
| **EWM** | لا يوجد | ✅ 10 features |
| **learning_rate** | 0.05 | 0.02 |
| **early_stopping_rounds** | 30 | 80 |
| **reg_lambda** | 1.0 | 5.0 |
| **Best Iteration** | 77 | 1425 |
| **Test MAE** | 23.79 | **12.94 ↓46%** |
| **Near Failure MAE** | 110.50 | **13.05 ↓88%** |

### المشاكل التي تم حلها

```
المشكلة 1: النموذج لا يعرف أين يُركّز (RUL=361 vs RUL=125 يُربكانه)
الحل:      Capping → كل شيء فوق 125 يصبح 125 → تركيز كامل على 0-125

المشكلة 2: النموذج لا يعرف "مجموع التعب" التاريخي للمحرك
الحل:      Cumulative Degradation → يحفظ تاريخ التآكل الكلي

المشكلة 3: النموذج لا يكشف التسارع السريع في التدهور
الحل:      EWM → الأحدث يوزن أكثر → يلاحظ التسارع بسرعة

المشكلة 4: النموذج يتوقف مبكراً (iteration=77 فقط!)
الحل:      early_stopping_rounds=80 + learning_rate=0.02
           → يواصل التحسين حتى iteration 1425
```

---

## كيفية تشغيل النظام

### 1. إعادة تدريب النموذج (اختياري)
```bash
python export_model_v2.py
```

### 2. تشغيل الـ Dashboard
```bash
streamlit run app.py
```

### 3. فتح في المتصفح
```
http://localhost:8501
```

---

*آخر تحديث: أبريل 2026 | النموذج: XGBoost v2 | الدقة: MAE=12.94 cycles*
